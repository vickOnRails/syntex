import React from "react";
import { Layout } from "../../components";

/**
 * Note - Renders individual note
 * @returns JSX>Element
 */
const Note = () => {
  return <Layout>Sigle Note</Layout>;
};

export default Note;
